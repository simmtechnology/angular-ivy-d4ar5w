import { JwPaginationComponent } from './jw-pagination-component';

describe('JwPaginationComponent', () => {
  it('should create an instance', () => {
    expect(new JwPaginationComponent()).toBeTruthy();
  });
});
