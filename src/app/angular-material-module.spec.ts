import { AngularMaterialModule } from './angular-material-module';

describe('AngularMaterialModule', () => {
  it('should create an instance', () => {
    expect(new AngularMaterialModule()).toBeTruthy();
  });
});
